enum AuthentificationStatus {
  authentificated,
  wrongCredentials,
  serverError,
  noInternet,
}

enum SmartContractType {
  payment,
  delivery,
}

enum SmartContractStatus {
  created,
  inProgress,
  done,
  error,
}
